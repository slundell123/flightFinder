Flight finder:
This program takes an input of two airport destinations (leaving and returning) as well as the date of travel and outputs possible flight choices for the user. These flights can be sorted based on what the user finds to be most important.

To run the program (You must be using a windows machine):
Compile the program: g++ -std=c++11 driver.cpp -o run
Run the program: ./run
